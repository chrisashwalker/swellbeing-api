name: CI checks

on: 
  push:
    branches:
      - main
  pull_request:
    branches-ignore:
      - renovate/**
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.14"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest ruff
    - name: Analyse and test the code
      run: |
        ruff check
        pytest
